# Vector = group of numbers

# "+" indicates an incomplete operation

# "Functions" are the tools we use in R, while "arguments" are the specifications for the function to use

# Typing "?" before a function can tell not only the arguments that can be used by a function, but the package (in {}) that needs to be installed/loaded to use the function

#the "vignettes" function will be bring up useful resources for some packages

# useful online resources: rbloggers, stackoverflow, rdocumentation